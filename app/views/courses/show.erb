<%= @course.title %>
Status:
<% if course_completed(@course) %>
    Completed 
<% else %>
    Not Yet Completed
<% end %>

<%= link_to("Edit Course",edit_course_path(@course)) %>



<% if course_completed(@course) %>
    <%= link_to 'Reset Course', reset_course_path(@course) %>
<% end %>
<%= link_to 'Edit', edit_course_path %>
<%= link_to 'Destroy', course_path(@course),data: {:confirm => 'Are you sure?'}, :method => :delete %>
